# Absolute URL of the deployed FastAPI backend (no trailing slash).
# Local dev leaves this empty and lets Vite proxy /api → http://localhost:8000.
# Example for production: https://pronunciation-coach-api.up.railway.app
VITE_API_URL=https://languagecoach-production.up.railway.app
